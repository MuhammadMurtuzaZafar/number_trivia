name: number_trivia
description: the project base on CLEAN Architecture


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'


dependencies:
  flutter:
    sdk: flutter



  cupertino_icons: ^1.0.2
  #service locator
  get_it:

  #state management
  flutter_bloc:

  #value
  equatable:

  #functional programing things
  dartz:

  #remote api
  connectivity:
  http:

  #local
  shared_preferences:

  mockito:


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7

  flutter_lints: ^2.0.0


flutter:


  uses-material-design: true

